name: Build Test

on:
  workflow_call:
  pull_request:
  push:
      
permissions:
  pull-requests: write
  issues: write
  repository-projects: write

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.24

      - name: Check out code
        uses: actions/checkout@v2

      - name: Format
        run: go fmt

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.24

      - name: Check out code
        uses: actions/checkout@v2

      - name: Build
        run: go build -i testapp

      - name: Bin
        uses: actions/upload-artifact@v2
        with:
          name: testapp
          path: ./testapp

